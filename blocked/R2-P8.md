# BLOCKED — R2-P8

## 状態

BLOCKED

## 決めたいこと

R2-P6の実装・検証が未完了の状態で、R2-P8の受入れ記録を確定し、RoadmapのR2行を完了へ更新するか。

## 根拠

- `TASKS.md:85-90` のR2-P6は `status: in_progress` で、完了条件は4行列・分割距離・`sampler2DArray`・境界ブレンド・安全なフォールバックの実装である。
- `Docs/Plans/RenderingR2SkyAtmosphereCsmPlan.md:43-46` はP5の後にP6を必須工程として置き、P8を完了コミットの工程としている。
- 現行 `Assets/Shaders/lighting.frag:46` は単一 `sampler2D shadowMap`、`Assets/Shaders/forward_transparent.frag:48` も単一 `sampler2D shadowMap` のままである。P6のGPUサンプリング移行を示す `sampler2DArray` の実装はない。
- `git log --all --oneline` にはP1〜P5とP7のコミットはあるが、P6の実装コミットはない。P7の受入れ自己検査はCPU参照契約とR2専用golden/thresholdを検証するが、未実装のGPUサンプリングを代替しない。

## 選択肢

1. R2-P6を先に実装・検証し、その実装コミットとP7再検証を含めてP8を再開する。
2. R2-P6をR2の非対象へ変更し、計画・完了条件・Roadmapを明示的に再設計する。
3. 未完了のP6を受入れ記録へ隠したままR2を完了扱いにする。

## 推奨

1を推奨する。R2の計画と現在のTASKSを変えず、P6の公開境界とGPU契約を実装・検証した後にだけ、R2受入れとRoadmap完了化を行えるためである。

## 再開条件

R2-P6の完了コミット、指定focused build/CTest、P7のR2専用検証ログを揃えたうえで、R2-P8の受入れ記録・Roadmap更新・R3新規M1入口を確定する。
