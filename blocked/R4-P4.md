# R4-P4: ray結果をirradiance/distance atlasへ積分して更新する

3回の反復で完了できなかったため、ランナーが `blocked` にした(run-id: 20260921-212812)。

## 直近の失敗

最後の信号: (信号なし)。反復の出力は `.harness/runs/20260921-212812/iter-3.out.txt`。

## 次にやること

- この理由が正しいか人が見る。タスクの切り方が大きすぎるなら `TASKS.md` で分割する。
- 解けたら `status:` を `todo` に戻す。

## 解消記録 (2026-09-22)

R4-P4を再開し、Debug buildと専用CTestが成功した。GPU readbackで単一平面、1段bounce、visibility weighting、hysteresis、border sampleを確認し、validation errorは0件だった。独立評価の`NEEDS_WORK`はCMake参照先のshader/testが未追跡だったことだけで、両ファイルをP4実装コミットへ含めて解消する。この記録は当時の停止理由を残す履歴であり、R4-P4はblockedではない。
