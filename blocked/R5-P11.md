# R5-P11: 動的TLAS更新とRT無効fallbackをGPU受入れする

## 再開条件

P10のRT stage修正を完了した。RT影passが実行時に読むGLSLはR5-P10の成果物として追跡し、P11の作業範囲にも明記した。P11は`TASKS.md`で`todo`へ戻した。

## 残っている受入れ項目

- `LightingParamsLayoutTest`のプリエクスポージャ契約を、R5のRasterHardShadow、RayTracingHardShadow、RasterFallbackを含む7 modeへ更新して通す。
- 動的TLAS captureとRT無効時のraster fallbackを再確認し、全CTestをP11開始時baselineと比較する。

## 既存の検証記録

- 初期影 frame 78、TLAS更新後 frame 80で旧領域が明るく新領域が暗くなり、frame 82のraster referenceとRT影、RT無効fallbackは`max_lsb=0`で一致した。証拠は`.harness/runs/20260921-073235/verify-R5-P11-1.txt`〜`verify-R5-P11-3.txt`。
- 反復前baselineは234件中8失敗、変更後は235件中7失敗。比較では新規失敗は確認されていない。`LightingParamsLayoutTest`の期待値は未修正のため、P11は未完了である。
