# 起動経路とRoadMapの一次確認を先に行う

起動後の画面だけを根拠に別シーンへ遷移したと判断せず、`GameApplicationHandler`から既存のGameMode、`Enter`のシーン構成、保存済みアセットの有無を順に確認する。保存済みのレンダリングシーンが存在しない場合は、期待するシーンを推測して起動経路へ追加しない。

また、無視対象の計画ファイルは実装後に状態が遅れることがあるため、RoadMapの依存関係と、追跡対象の受入れ記録・進捗・完了トレーラーを突き合わせて、実装済みフェーズと帳簿のずれを明示する。
